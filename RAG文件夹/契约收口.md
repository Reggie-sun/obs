
现在是不是要做契约收口

对，现在该做了。

但不是“把所有接口都冻结”，而是先做一轮 **V1 主契约收口**。原因很简单：你现在 OCR、SOP 直生、logs/config/ops、trace/snapshot、rerank 路由、轻记忆都已经接上了，再往后继续加企业微信、DB 真源、metrics，会把字段和语义继续冲散。

我建议现在先收这几组稳定面：

- 用户主链路：/documents、/jobs、/retrieval/search、/chat/ask、/chat/ask/stream、/sops/generate/document、导出下载
- 运行治理：/system-config、/ops/summary、/logs
- 统一字段口径：document_id/doc_id、mode/default/effective、status/stage、rerank_strategy/provider/model、prompt_budget、分页结构、时间戳格式、错误返回格式

不建议现在冻结的：

- rerank-compare
- trace
- snapshot/replay
- OCR artifact 细节
- 各种诊断型 detail 字段  
    这些还属于内部调试面，后面还会变。

如果按优先级排，下一步最值当的是：

1. 先收 chat / retrieval / sop 契约
2. 再收 system-config / ops / logs
3. 最后给这些接口补一层 contract tests 和示例响应

一句话：  
**是，现在应该做契约收口，而且要在继续上企业微信、DB 真源之前做。**

如果你要，我下一步可以直接开始这轮收口：先从 chat + retrieval + sop 的 schema 和错误语义下手。


**改v1 plan

请以 /home/reggie/vscode_folder/Enterprise-grade_RAG/MAIN_CONTRACT_MATRIX.md 作为当前 V1 主契约来源（source of truth），更新 V1_PLAN 中与“冻结接口 / 主契约 / 诊断面”相关的章节。

要求：
1. 不要重复抄写整个 Contract Matrix
2. 只把 V1_PLAN 里和主契约收口有关的规则、范围、变更约束补齐
3. 明确：
   - 哪些接口属于 V1 已冻结主契约
   - 哪些属于诊断面，暂不冻结
   - 稳定字段禁止改名、禁止删除，只允许新增可选字段
4. 保持 V1_PLAN 作为“版本规划与收口规则文档”，不要把它改写成字段明细表
5. 如发现 V1_PLAN 与 MAIN_CONTRACT_MATRIX.md 有冲突，以 MAIN_CONTRACT_MATRIX.md 为准，并列出冲突点
先给出修改计划，再动文档



**改代码

实现或修改代码时，必须以 /home/reggie/vscode_folder/Enterprise-grade_RAG/MAIN_CONTRACT_MATRIX.md 为当前 V1 主契约约束。并且用skill
/home/reggie/vscode_folder/Enterprise-grade_RAG/.claude/skills/auto-feature-smoke-test/SKILL.md
要求：
1. 不允许修改 Matrix 中定义的稳定字段名和响应骨架
2. 不允许删除稳定字段
3. 只允许新增可选字段
4. 诊断字段不得变成前端主流程依赖
5. 如果改动涉及主契约冲突，先停止并列出冲突点，不要直接改代码




实现或修改 /home/reggie/vscode_folder/Enterprise-grade_RAG 仓库代码时，必须以：
1. /home/reggie/vscode_folder/Enterprise-grade_RAG/MAIN_CONTRACT_MATRIX.md
作为当前 V1 主契约基线
2. /home/reggie/vscode_folder/Enterprise-grade_RAG/V1_PLAN.md
作为版本边界与迭代范围约束

强制执行规则：

A. 主契约冻结规则
- 不允许修改 Matrix 中定义的稳定字段名
- 不允许修改 Matrix 中定义的响应骨架
- 不允许删除任何稳定字段
- 只允许新增“可选字段”，且不得破坏现有前端兼容性
- 诊断字段/debug字段/trace字段不得成为前端主流程依赖

B. 改动前必须先输出 Contract Check
在开始改代码前，必须先判断并输出：
【Contract Check】
- 是否修改 API 响应字段：是/否
- 是否修改稳定字段名：是/否
- 是否修改响应骨架：是/否
- 是否删除稳定字段：是/否
- 是否新增字段：是/否（若是，说明是否可选）
- 是否让诊断字段进入前端主流程：是/否

C. 冲突处理规则
若改动涉及主契约冲突：
- 立即停止，不要直接改代码
- 先列出冲突点
- 列出受影响接口、字段、前端影响面、测试影响面
- 给出不破坏主契约的替代方案
- 等我确认后再继续

D. 实现边界
- 优先通过内部实现、服务层、适配层、兼容层解决问题
- 若需求与主契约冲突，不得直接改接口返回结构来“顺手修掉”
- 不得为了调试方便，把诊断字段升级为业务字段

E. 交付时必须说明
每次完成改动后，必须单独说明：
1. 是否触碰 MAIN_CONTRACT_MATRIX.md 稳定契约：是/否
2. 是否新增可选字段：若有，列出
3. 是否存在前端兼容风险：是/否
4. 本次验证覆盖了哪些测试