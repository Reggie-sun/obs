
**执行清单**

先别急着改 chunk_size 或 rerank，按这个顺序判断：

1. 先确认当前 runtime 能不能做“可比评估”
    - 跑 make require-shared-data-visibility
    - 跑 make dev-local-api
    - 看 /api/v1/health 里的这些字段是否正常：
        - active_documents_missing_vectors = 0
        - vector_index_consistency_status = ok
        - chunk_config 和你预期一致
    - 再跑 python scripts/eval_retrieval.py --sample-track frozen --sample-statuses verified
    - 如果这里出现大面积 0% recall、期望 doc_id 在 runtime 里已不存在、或和 frozen baseline 完全不可比，那先判定为 runtime/corpus drift，不要进入 chunk 优化
2. 再判断问题属于 cleaning 还是 chunk
    - 如果现象是：
        - 命中文档都不对
        - 命中的都是新 family / 新 doc_id
        - top-k 全体偏到当前库里的另一批文档  
            先归因为 corpus / ACL / metadata / baseline drift
    - 如果现象是：
        - expected_doc 能进 top-k
        - 但 top1 总是摘要块、泛化说明、半截段落
        - citation 老是糊在 summary 上  
            归因为 chunk / hierarchy / representation
    - 如果现象是：
        - parsed 文本里大量重复页脚、页码、glyph、暂无、表格碎片
        - 这些噪声真的进入 chunk  
            归因为 screening / cleaning
3. 用文档证据确认是不是值得开 Phase 2B
    - 看 data/parsed/<doc_id>.txt
    - 看 data/chunks/<doc_id>.json
    - 看 data/screening_reports/<doc_id>.json
    - 如果你看到的是重复 footer / slide artifact / placeholder residue，这条线可以继续打 PreStockScreeningService
    - 如果 parsed 已经很干净，但 chunk 还是“文档对、块不对”，就别继续扩 cleaning，直接进 chunk/hierarchy

**你现在这套结构能做什么**

- 可以支撑 structured chunk 优化：因为已经有 doc_summary / section_summary / clause / table、parent_id、retrieval_text 这些信号，而且 retrieval 已经在消费它们。
- 可以支撑 screening canary：因为入库链里已经有 screening -> cleaning -> chunk、report、noise profile。
- 还不适合盲目做大范围检索结论：因为仓库当前状态把最大 blocker 定义成 NOT COMPARABLE，不是 chunk 能力缺失。

**怎么判断该改哪边**

- 改 screening/cleaning 的信号：
    - chunk 里混入大量机械噪声
    - 噪声块能解释 citation/top chunk 失真
    - 同类噪声在原始上传 replay 里可稳定复现
- 改 chunk/hierarchy 的信号：
    - 文档对了，但 clause 老输给 summary
    - table 命中进了 top-k，但最终回答块不对
    - clean control 文档也有同样问题
- 先别改的情况：
    - frozen eval 和 live runtime 不可比
    - baseline manifest 对应文档在当前 runtime 已经删了
    - ACL / metadata 还没 seed 对齐

**最小落地路径**

- 第一步跑：make require-shared-data-visibility
- 第二步跑：make dev-local-api
- 第三步跑：python scripts/eval_retrieval.py --sample-track frozen --sample-statuses verified
- 如果通过，再挑 3 份代表性文档人工看：
    - 1 份 noisy PDF
    - 1 份 PPTX/SOP
    - 1 份 clean control
- 然后只做一个判断：
    - runtime drift
    - screening problem
    - chunk/hierarchy problem

这次是仓库内分析，没有改代码，也没有新跑测试；依据是现有实现、测试、retrieval-state、Phase 2B cleaning audit 和 eval 脚本。下一步我可以直接替你跑这 3 步，并把结果收成“你现在更适合改 cleaning 还是改 chunk”的结论。

4:12 PM

let user know

4:17 PM

Worked for 25s

**这窗口刚刚做了什么**

- 你的目标是判断：你现在的清洗和 chunk 结构，够不够支撑后面做检索优化。
- 我没有直接给主观判断，而是先去读了仓库里跟 retrieval / ingestion / chunk / eval 相关的主规则、现状文档、实现代码和测试。
- 然后我把两件事分开了看：
    - 从“结构能力”上看，你现在这套链路已经有继续优化检索的基础。
    - 从“现在能不能立刻可靠评估优化效果”上看，仓库当前还有 runtime / corpus 可比性问题。
- 接着我又给了你一份实际可执行的检查清单，告诉你先判断是：
    - runtime drift
    - screening/cleaning 问题
    - 还是 chunk/hierarchy 问题

**名词解释**

- runtime-workflow：这次我用它的思路来做仓库内分析。简单说，就是先按仓库自己的流程判断问题，不靠拍脑袋给结论。
- retrieval-state：这是仓库里记录“当前检索到底卡在哪”的状态文档。这次它很关键，因为它提示现在最大的 blocker 不是 chunk 太差，而是 live 环境和基线可能不可比。
- frozen baseline：可以理解成“冻结过的一份标准答案和标准环境口径”。之后所有优化都应该拿它做前后对比，不然你会分不清是优化生效了，还是语料/环境变了。
- NOT COMPARABLE：意思不是“检索一定变差了”，而是“当前结果和旧基线已经没法公平比较”。这在这次窗口里很重要，因为它决定了现在不适合直接下结论说该改 rerank 还是改 chunk。
- Phase 2B：这是仓库里“先做 ingest 侧 cleaning / screening canary”的阶段名。白话就是：先小范围验证清洗到底是不是当前主瓶颈，而不是一上来大改 chunk。
- PreStockScreeningService：这是你现在真正负责“预入库去噪”的主服务，比单纯的机械 clean_text 更重要。它会决定哪些内容保留、降权、或丢弃。
- structured chunk：不是只按字符硬切，而是把文档切成像 doc_summary、section_summary、clause、table 这种更有语义的块。它之所以重要，是因为 retrieval 端已经真的在利用这些结构信号做排序和回补。
- parent_id：就是 chunk 之间的父子关系。比如命中一个细条款时，可以顺着它把上级 section/doc summary 补回来，帮助回答更完整。

**还没完成的部分**

- 目前我做的是仓库内分析和归因，没有实际替你跑那组检查命令。
- 所以现在还没有最终落到“你的当前数据更像 cleaning 问题还是 chunk 问题”的实测结论。
- 下一步最直接的动作，就是按我给你的 3 步去跑：
    - make require-shared-data-visibility
    - make dev-local-api
    - python scripts/eval_retrieval.py --sample-track frozen --sample-statuses verified