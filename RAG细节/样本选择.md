

**这窗口刚刚做了什么**

这几轮我一直在做一件事：把 portal/library 的检索语料先治理干净，而不是去改 retrieval 排序。

这轮最新结果是：

- 我把真实 runtime 里的 portal/library 文档重新梳理了一遍，最后把 active corpus 收口成：
    - include=7
    - family_merge=2
    - exclude=40
    - candidate=0
- 原来最后还留着 1 份 candidate 文档：
    - 601车间级预装中心（一期）管控系统架构设计方案.docx
- 我没有直接放弃它，而是继续做 live probe，发现：
    - 用默认标题/章节 query，它会漂
    - 但用更强的、文档特征更明确的 query，它能稳定命中自己
- 所以我做了一个更干净的处理：
    - 不是把它留在 candidate
    - 而是给它配了两条治理层明确声明的 curated query
    - 然后把它重新拉回 rolling
- 现在这条治理链是闭环的：
    - snapshot
    - governance manifest
    - retrieval samples
    - generator
    - precondition tests
    - 都对齐了

当前状态是：

- active rolling 样本：18
- active candidate 样本：0
- alignment report：mismatch_count=0
- 相关测试：34 passed

**名词解释**

- rolling: 这里就是“当前库里这批文档的观察集”。它不是正式 gate，但用来盯现在这批语料是否还能被搜到。
- candidate: 就是“先观察、先别并入主观察集”的候选区。之前有一些文档放这里，现在已经清空了。
- family_merge: 指“同一份内容有多个 active 版本”，比如 pdf/pptx。这类不算检索退化，而是按 family truth 接受。
- exclude: 指“这份文档虽然在库里，但不该拿来定义检索健康度”，常见是报价、历史稿、参考资料、招标文件之类。
- curated query: 不是随便从标题里自动长出来的 query，而是根据文档正文特征，人工挑出来、并正式写进治理规则的 query。它这次很关键，因为 01303 这份文档默认 query 不稳，但 curated query 稳。
- alignment report: 就是检查 snapshot / governance / retrieval_samples 三者有没有打架。这次结果是对齐的。
- checked-in diff: 就是“如果现在按规则重新生成样本，会不会和仓库里已经提交的样本文件不一致”。这次是 clean。

**还没完成的部分**

主线其实已经差不多收口了。现在不再是“规则没建好”，而是“要不要继续把这 40 份 exclude 再压缩成更少的分类规则”。如果继续做，下一个更自然的动作就是把这些 exclude 文档再归并成更稳定的规则桶，而不是继续加样本。