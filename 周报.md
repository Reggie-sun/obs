## 项目周报（2026.03.31 - 2026.04.06）

**项目**: Enterprise-grade RAG（企业级检索增强生成系统） **分支**: `feature/retrieval-priority`（领先 main 24 个 commit）

---

### 一、Retrieval 检索优化（核心工作）

**当前阶段**: Phase 1B-B — Gate 复核完成，结论为 **GATE NOT PASSED**

**已完成**:

- 完成 cross-dept supplemental trigger / top-k blocker 收口，修复了跨部门文档检索漏召回问题
- 在 retrieval_service.py 中新增 exact/fine query 的 top1 literal coverage 低质量保护逻辑
- baseline 恢复并可在 fresh API 上稳定复现（两次独立运行结果一致）
- 样本从 31 条扩充至 43 条（新增 boundary/lexical/hybrid/cross-dept 样本）
- 建立完整 eval 框架：`scripts/eval_retrieval.py` + 22 次 eval 运行记录
- eval ACL seed 体系就位，支持真实部门 token 测试

**当前基线指标**（43 条样本）:

|指标|值|
|---|---|
|top1_accuracy|95.35%|
|topk_recall|97.67%|
|supplemental_precision|0.957|
|supplemental_recall|0.957|

**发现的待解决问题**:

- `cross-013`: department_sufficient 误判，导致跨部门文档被遗漏
- `sop-005`: provisional 阈值在非确定性向量搜索下存在振荡问题
- 阈值临界区（0.45-0.75）样本覆盖不足

### 二、前端 Portal 改进

- 重构 LoginPage 登录页面，新增 welli-login-logo-v2
- 优化 PortalChatPage 聊天页面交互
- 新增 PortalSopGeneratePanel SOP 生成面板功能
- 优化 PortalShell 布局和 PortalDocumentCenter 文档中心
- 引入 UI 组件库：Button、Card、Input、ResultBox、StatusPill、EvidenceSourceSummary
- 统一设计系统：tailwind.config.js + index.css 全局样式

### 三、后端工程化

- 新增 retrieval diagnostics 功能（诊断 API）
- 新增 document upload format 支持
- 完善 agent harness 系统（`.agent/harness/` + `scripts/agent_harness.py`）
- 新增 `scripts/rebuild_documents.py` 文档重建脚本
- 新增 `scripts/check_retrieval_state_sync.py` 状态同步检查
- Dockerfile 更新，Makefile 增加 agent 相关 target

### 四、测试覆盖

- 新增 `test_retrieval_chat.py`（504 行，检索聊天测试）
- 新增 `test_structured_chunker.py`（714 行，结构化切块测试）
- 新增 `test_retrieval_eval_preconditions.py`（eval 前置条件测试）
- 新增 `test_retrieval_state_sync_guard.py`（状态同步守卫测试）
- 更新 system_config 相关测试

### 五、文档与规范

- 建立 AGENTS.md 工作流规范（632 行），定义 spec→plan→implement→review 主线
- 编写 RETRIEVAL_OPTIMIZATION_PLAN / BACKLOG 等检索优化文档
- 编写 OPS_CHUNK_REBUILD_RUNBOOK 运维手册
- 建立 retrieval state snapshot 机制（`.agent/context/retrieval-state.md`）