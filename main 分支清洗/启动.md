
# 1) 当前 shell 里准备 branch-local API/FE 运行时
export REMOTE_HOST=192.168.10.200
export COMPOSE_PROJECT_NAME=enterprise-rag-cleaning
export API_CONTAINER_NAME=enterprise-rag-api-cleaning
export API_PORT=18020
export FE_PORT=3100
export HOST_SHARED_DATA_DIR="$HOME/mnt/enterprise-rag-shared-data"

# 2) 共享远端 infra
export RAG_CELERY_BROKER_URL="redis://${REMOTE_HOST}:6380/0"
export RAG_CELERY_RESULT_BACKEND="redis://${REMOTE_HOST}:6380/1"
export RAG_POSTGRES_METADATA_DSN="postgresql://root:123456@${REMOTE_HOST}:5433/welllihaidb?schema=public"
export DATABASE_URL="$RAG_POSTGRES_METADATA_DSN"
export RAG_QDRANT_URL="http://${REMOTE_HOST}:6333"

# 3) 队列名要跟你共享的 worker 对齐
# 如果你共享的是“已经存在的 branch workers”，就填它们的 queue 名
export RAG_CELERY_INGEST_QUEUE=ingest_bugfix
export RAG_CELERY_INGEST_CPU_QUEUE=ingest_bugfix_cpu
export RAG_CELERY_INGEST_OCR_QUEUE=ingest_bugfix_ocr
export RAG_CELERY_PREVIEW_QUEUE=preview_bugfix
export RAG_CELERY_FOLDER_DELETE_QUEUE=folder_delete_bugfix

先把远端 shared-data 挂到本机，再起 API；这一步很重要，不然上传、清洗、预览都容易假失败：

`mkdir -p "$HOST_SHARED_DATA_DIR" sshfs lenovo@192.168.10.200:/srv/enterprise-rag/data "$HOST_SHARED_DATA_DIR" -o allow_other ./scripts/check_local_shared_data_visibility.sh`

API_PORT=8020 docker compose up -d api

cd frontend
VITE_API_TARGET="http://127.0.0.1:8020" npm run dev -- --host 0.0.0.0 --port "3000" --strictPort